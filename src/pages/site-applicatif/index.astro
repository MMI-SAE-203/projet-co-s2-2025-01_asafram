---
import LayoutApplicatif from "../../layouts/LayoutApplicatif.astro";
import CardProduits from "../../components/site-applicatif/CardProduits.astro";
import BulleCategorie from "../../components/site-applicatif/BulleCategorie.astro";
import {
  getProductsByContinent,
  getRecipeById,
  getPopularRecipes,
  getPopularProducts,
} from "../../../backend/backend.js";
import IconeBoissons from "../../assets/icones/icone-boissons.webp";
import IconeFruitsEtLegumes from "../../assets/icones/icone-fruits.webp";
import IconeViandes from "../../assets/icones/icone-viandes.webp";
import IconeEpices from "../../assets/icones/icone-epices.webp";
import IconeSnacks from "../../assets/icones/icone-snacks.webp";
import SvgAsie from "../../assets/icones/continent-asie.svg";
import SvgAfrique from "../../assets/icones/continent-afrique.svg";
import SvgAmeriqueLatine from "../../assets/icones/continent-ameriquelatine.svg";
import SvgMaghreb from "../../assets/icones/continent-maghreb.svg";
import CardRecettes from "../../components/site-applicatif/CardRecettes.astro";
import ImageCarrousel1 from "../../assets/images/image-carrousel1.webp";
import ImageCarrousel2 from "../../assets/images/image-carrousel2.webp";

// Définition des catégories
const categories = [
  { nom: "Boissons", icone: IconeBoissons },
  { nom: "Fruits et Légumes", icone: IconeFruitsEtLegumes },
  { nom: "Viandes", icone: IconeViandes },
  { nom: "Épices", icone: IconeEpices },
  { nom: "Snacks", icone: IconeSnacks },
];

// Actualités pour le carousel
const actualites = [
  {
    id: 1,
    titre: "NOUVEAU MAGASIN PARTENAIRE À LYON",
    description: "Découvrez notre tout nouveau point de vente chez Épicerie Bio Lyon 7ème. Plus de 200 produits ASAFRAM maintenant disponibles !",
    image: ImageCarrousel1.src,
  },
  {
    id: 2,
    titre: "PRODUIT VEDETTE : THÉ VERT DU JAPON",
    description: "Notre thé vert matcha premium du Japon est en promotion à -25%. Qualité cérémoniale, riche en antioxydants, saveur authentique !",
    image: "https://picsum.photos/1200/400?random=25",
  },
  {
    id: 3,
    titre: "NOUVELLE NEWSLETTER MENSUELLE",
    description: "Abonnez-vous à notre newsletter 'Saveurs du Monde' : recettes exclusives, tips de nos chefs partenaires et offres privilégiées",
    image: ImageCarrousel2.src,
  },
];

// Récupération des produits asiatiques
const asianProductsResult = await getProductsByContinent("Asie", {
  perPage: 8,
});

const asianProducts = asianProductsResult.items.map((produit) => {
  produit.distance = 15;
  if (produit.nombres_notes && !produit.nbNotes) {
    produit.nbNotes = produit.nombres_notes;
  }
  if (!produit.nbNotes) {
    produit.nbNotes = Math.floor(Math.random() * 50) + 10;
  }
  return produit;
});

// Récupération des produits populaires
const popularProducts = await getPopularProducts(8);

// Récupération des recettes populaires
const popularRecipes = await getPopularRecipes(8);
---

<LayoutApplicatif title="ASAFRAM" description="Carte produit démo">
  <div class="bg-white min-h-screen">
    <!-- Section avec fond background -->
    <div class="bg-background">
      <!-- Carousel Actualités -->
      <section class="relative bg-gradient-to-br from-orange-100 to-amber-50 overflow-hidden">
        <div 
          x-data="{ currentSlide: 0, slides: 3 }" 
          x-init="setInterval(() => { currentSlide = (currentSlide + 1) % slides }, 5000)"
          class="relative h-80 md:h-96"
        >
          {actualites.map((actualite, index) => (
            <div 
              x-show={`currentSlide === ${index}`}
              x-transition:enter="transition ease-in-out duration-500"
              x-transition:enter-start="opacity-0 translate-x-full"
              x-transition:enter-end="opacity-100 translate-x-0"
              x-transition:leave="transition ease-in-out duration-500"
              x-transition:leave-start="opacity-100 translate-x-0"
              x-transition:leave-end="opacity-0 -translate-x-full"
              class="absolute inset-0 w-full h-full"
            >
              <div class="relative w-full h-full">
                <img 
                  src={actualite.image} 
                  alt={actualite.titre}
                  class="w-full h-full object-cover"
                />
                <div class="absolute inset-0 bg-opacity-20"></div>
                <div class="absolute inset-0 flex items-center justify-center">
                  <div class="text-center text-white max-w-4xl px-4">
                    <h2 class="text-3xl md:text-5xl font-radio-canada-big font-black mb-4 tracking-wide">
                      {actualite.titre}
                    </h2>
                    <p class="text-lg md:text-xl font-raleway mb-6">
                      {actualite.description}
                    </p>
                    <button class="bg-sable hover:bg-orange-200 text-black font-bold px-8 py-3 rounded-xl uppercase tracking-wide transition-colors duration-300">
                      En savoir plus
                    </button>
                  </div>
                </div>
              </div>
            </div>
          ))}

          <!-- Indicateurs de slide -->
          <div class="absolute bottom-6 left-1/2 transform -translate-x-1/2 flex space-x-2">
            {actualites.map((_, index) => (
              <button 
                x-on:click={`currentSlide = ${index}`}
                class="w-3 h-3 rounded-full transition-colors duration-300"
                :class={`currentSlide === ${index} ? 'bg-white' : 'bg-white bg-opacity-50'`}
              ></button>
            ))}
          </div>

          <!-- Flèches de navigation -->
          <button 
            x-on:click="currentSlide = currentSlide === 0 ? slides - 1 : currentSlide - 1"
            class="absolute left-6 top-1/2 transform -translate-y-1/2 bg-sable hover:bg-orange-200 text-black p-3 rounded-full transition-all duration-300 shadow-lg hover:shadow-xl"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="3">
              <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"></path>
            </svg>
          </button>
          <button 
            x-on:click="currentSlide = (currentSlide + 1) % slides"
            class="absolute right-6 top-1/2 transform -translate-y-1/2 bg-sable hover:bg-orange-200 text-black p-3 rounded-full transition-all duration-300 shadow-lg hover:shadow-xl"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="3">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"></path>
            </svg>
          </button>
        </div>
      </section>

      <!-- Layout Continents Alignés -->
      <section class="container mx-auto px-8 py-16">
        
        <div class="flex flex-wrap justify-center items-center gap-40 py-24">
          <!-- Asie -->
          <a 
            href="/site-applicatif/produits?continent=Asie"
            class="group flex flex-col items-center cursor-pointer"
          >
            <div class="relative mb-8 p-8 rounded-full bg-gray-50 group-hover:bg-white transition-all duration-500 group-hover:shadow-2xl group-hover:shadow-gray-400/30">
              <img 
                src={SvgAsie.src} 
                alt="Asie" 
                class="w-40 h-40 group-hover:scale-110 group-hover:rotate-2 transition-all duration-500 ease-out opacity-80 group-hover:opacity-100"
              />
            </div>
            <h3 class="text-2xl font-bold text-gray-800 uppercase tracking-widest group-hover:text-black group-hover:scale-105 transition-all duration-300">ASIE</h3>
          </a>

          <!-- Maghreb -->
          <a 
            href="/site-applicatif/produits?continent=Maghreb"
            class="group flex flex-col items-center cursor-pointer"
          >
            <div class="relative mb-8 p-8 rounded-full bg-gray-50 group-hover:bg-white transition-all duration-500 group-hover:shadow-2xl group-hover:shadow-gray-400/30">
              <img 
                src={SvgMaghreb.src} 
                alt="Maghreb" 
                class="w-40 h-40 group-hover:scale-110 group-hover:-rotate-2 transition-all duration-500 ease-out opacity-80 group-hover:opacity-100"
              />
            </div>
            <h3 class="text-2xl font-bold text-gray-800 uppercase tracking-widest group-hover:text-black group-hover:scale-105 transition-all duration-300">MAGHREB</h3>
          </a>

          <!-- Afrique -->
          <a 
            href="/site-applicatif/produits?continent=Afrique"
            class="group flex flex-col items-center cursor-pointer"
          >
            <div class="relative mb-8 p-8 rounded-full bg-gray-50 group-hover:bg-white transition-all duration-500 group-hover:shadow-2xl group-hover:shadow-gray-400/30">
              <img 
                src={SvgAfrique.src} 
                alt="Afrique" 
                class="w-40 h-40 group-hover:scale-110 group-hover:rotate-1 transition-all duration-500 ease-out opacity-80 group-hover:opacity-100"
              />
            </div>
            <h3 class="text-2xl font-bold text-gray-800 uppercase tracking-widest group-hover:text-black group-hover:scale-105 transition-all duration-300">AFRIQUE</h3>
          </a>

          <!-- Amérique Latine -->
          <a 
            href="/site-applicatif/produits?continent=Amérique Latine"
            class="group flex flex-col items-center cursor-pointer"
          >
            <div class="relative mb-8 p-8 rounded-full bg-gray-50 group-hover:bg-white transition-all duration-500 group-hover:shadow-2xl group-hover:shadow-gray-400/30">
              <img 
                src={SvgAmeriqueLatine.src} 
                alt="Amérique Latine" 
                class="w-40 h-40 group-hover:scale-110 group-hover:-rotate-1 transition-all duration-500 ease-out opacity-80 group-hover:opacity-100"
              />
            </div>
            <h3 class="text-xl font-bold text-gray-800 uppercase tracking-widest group-hover:text-black group-hover:scale-105 transition-all duration-300">AMÉRIQUE LATINE</h3>
          </a>
        </div>
      </section>
    </div>

    <!-- Section blanche pour les catégories et le reste -->
    <div class="bg-white">
      <!-- Section des catégories -->
      <section class="container mx-auto px-4 py-16">
        <ul class="flex flex-wrap justify-center gap-32 my-8">
          {
            categories.map((categorie) => (
              <li>
                <BulleCategorie nom={categorie.nom} icone={categorie.icone.src} />
              </li>
            ))
          }
        </ul>
      </section>

      <!-- Section des produits populaires -->
      <section class="container mx-auto px-4 py-8">
        <header class="mb-16">
          <h2 class="text-2xl !text-left font-raleway font-extrabold uppercase tracking-wider relative inline-block pb-4">
            Produits populaires
          </h2>
          <div class="w-full h-0.5 bg-black mt-2"></div>
        </header>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-14 justify-items-center">
          {popularProducts.map((produit) => <CardProduits product={produit} />)}
        </div>
      </section>

      <!-- Section des produits asiatiques -->
      <section class="container mx-auto px-4 py-8">
        <header class="mb-16">
          <h2 class="text-2xl !text-left font-raleway font-extrabold uppercase tracking-wider relative inline-block pb-4">
            Produits Asiatiques
          </h2>
          <div class="w-full h-0.5 bg-black mt-2"></div>
        </header>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-14 justify-items-center">
          {asianProducts.map((produit) => <CardProduits product={produit} />)}
        </div>
        <div class="flex justify-center mt-20">
          <a
            href="/site-applicatif/produits"
            class="bg-saumon hover:bg-saumon-hover text-black font-bold uppercase py-3 px-10 rounded-lg transition-colors duration-300 cursor-pointer"
          >
            Tout afficher
          </a>
        </div>
      </section>

      <!-- Section des recettes populaires -->
      <section class="container mx-auto px-4 py-8 pb-40">
        <header class="mb-16">
          <h2 class="text-2xl !text-left font-raleway font-bold uppercase tracking-wider relative inline-block pb-4">
            Recettes populaires
          </h2>
          <div class="w-full h-0.5 bg-black mt-2"></div>
        </header>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-14 justify-items-center">
          {popularRecipes.map((recette) => <CardRecettes recette={recette} />)}
        </div>
        <div class="flex justify-center mt-20">
          <a
            href="/site-applicatif/recettes"
            class="bg-jaune hover:bg-jaune-hover text-black font-bold uppercase py-3 px-10 rounded-lg transition-colors duration-300 cursor-pointer"
          >
            Tout afficher
          </a>
        </div>
      </section>
    </div>
  </div>

  <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
</LayoutApplicatif>
