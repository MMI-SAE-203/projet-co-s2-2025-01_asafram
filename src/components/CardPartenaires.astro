---
const { image, alt, description } = Astro.props;
---

<div class="border rounded-lg flex flex-col items-center justify-center w-50 h-50 overflow-hidden p-4 shadow-sm m-4 transition-all hover:shadow-md">
    <img class="w-16 mb-4" src={image.src} alt={alt}>
    <p class="text-center uppercase font-bold">{description}</p>
</div>